# Vehicle Purchase Calculator

A single-page, no-dependencies calculator comparing net worth after N years
across cash, loan/HP, PCP (keep or return), and lease purchase methods for a
vehicle. Open `index.html` directly in a browser — no build step, no server.

Full usage and worked-example verification: see below (added in a later commit).
